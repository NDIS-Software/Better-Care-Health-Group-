# Interactive Reviews Carousel 独立预览

这是从附件代码整理出的独立静态页面，不依赖主项目。支持悬停减速、鼠标或触控拖拽、惯性释放和卡片指针反馈。

## 运行

直接双击 `index.html` 即可预览。

如果浏览器限制本地资源，也可以在当前目录运行：

```powershell
python -m http.server 8080
```

然后访问 <http://localhost:8080>。

头像来自 DiceBear 在线接口，离线时头像可能无法显示，但滚动效果不受影响。
